Add the Originkit "Liquid Distortion" component with the CLI:

npx originkit@latest add liquid-distortion --prompt

Authenticate with ORIGINKIT_API_KEY=cmp_live_wZ44cGjZ897WuqnLmiDIYMwUy5AdJpbD (export it in the shell before running the command).



Add the Originkit "Smoky Text" component with the CLI:

npx originkit@latest add smokytext --prompt

Authenticate with ORIGINKIT_API_KEY=cmp_live_wZ44cGjZ897WuqnLmiDIYMwUy5AdJpbD (export it in the shell before running the command).


Add the Originkit "Hero 10" section with the CLI:

npx originkit@latest add hero-10 --prompt

Authenticate with ORIGINKIT_API_KEY=cmp_live_wZ44cGjZ897WuqnLmiDIYMwUy5AdJpbD (export it in the shell before running the command).


Add the Originkit "Text Morph" component with the CLI:

npx originkit@latest add textmorph --prompt

Authenticate with ORIGINKIT_API_KEY=cmp_live_wZ44cGjZ897WuqnLmiDIYMwUy5AdJpbD (export it in the shell before running the command).



Add the Originkit "Text Morph" component with the CLI:

npx originkit@latest add textmorph --prompt

Authenticate with ORIGINKIT_API_KEY=cmp_live_wZ44cGjZ897WuqnLmiDIYMwUy5AdJpbD (export it in the shell before running the command).



Add the Originkit "Sticker Peel" component with the CLI:

npx originkit@latest add sticker-peel --prompt

Authenticate with ORIGINKIT_API_KEY=cmp_live_wZ44cGjZ897WuqnLmiDIYMwUy5AdJpbD (export it in the shell before running the command).


Add the Originkit "Live Chat" component with the CLI:

npx originkit@latest add live-chat --prompt

Authenticate with ORIGINKIT_API_KEY=cmp_live_wZ44cGjZ897WuqnLmiDIYMwUy5AdJpbD (export it in the shell before running the command).


Add the Originkit "Live Chat" component with the CLI:

npx originkit@latest add live-chat --prompt

Authenticate with ORIGINKIT_API_KEY=cmp_live_wZ44cGjZ897WuqnLmiDIYMwUy5AdJpbD (export it in the shell before running the command).


